import type { ButtonHTMLAttributes } from 'react'
import { cn } from '../../lib/utils'
export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> { variant?: 'default' | 'outline' | 'ghost' | 'destructive'; size?: 'default' | 'sm' | 'icon' }
export function Button({ className, variant = 'default', size = 'default', ...props }: ButtonProps) { return <button className={cn('inline-flex items-center justify-center rounded-lg text-sm font-medium transition focus:outline-none focus:ring-2 focus:ring-emerald-500 disabled:pointer-events-none disabled:opacity-50', variant === 'default' && 'bg-[#06C755] text-white hover:bg-[#05b34d]', variant === 'outline' && 'border border-slate-200 bg-white text-slate-700 hover:border-[#06C755] hover:text-[#06C755]', variant === 'ghost' && 'text-slate-500 hover:bg-slate-100 hover:text-slate-800', variant === 'destructive' && 'bg-red-500 text-white hover:bg-red-600', size === 'default' && 'h-9 px-4', size === 'sm' && 'h-8 px-3 text-xs', size === 'icon' && 'h-9 w-9', className)} {...props} /> }
